# 0033: インストール配置と、エンジンのオンデマンド起動

決定 0015 は「最初のクライアントが未起動ならエンジンを起動する」と決めていたが、
**どこにある実行ファイルを起動するのか**が未確定だったため実装できずにいた。
ここを詰めて、決定 0015 を実装可能にする。

これが解けないと**製品として動かない**。TSF クライアントはエンジンが既に起動して
いなければ繋がれず、ユーザーが手動で起動する運用は成り立たない。

## インストール配置

```
%ProgramFiles%\Ohagey\
    OhageyTSF.dll                       TSF テキストサービス(regsvr32 で登録)
    OhageyEngine.exe                    変換サーバー(決定 0004)
    OhageySettings.exe                  設定アプリ(決定 0013)
    models\
        ggml-model-Q5_K_M.gguf          Zenzai の重み(決定 0008)
    backends\
        cpu\    llama.dll ggml*.dll     決定 0028
        cuda\   ...
        vulkan\ ...
```

`%ProgramFiles%` 配下なのは既に決まっている(決定 0008 のモデル配置)。
**DLL と exe を同じディレクトリに置く**のがこの決定の実質的な中身。

> **DLL は `OhageyTSF.dll` としてビルドされる**(対応済み)。`.vcxproj` に
> `TargetName` を足しただけで、プロジェクト名・ディレクトリ・GUID は vendoring 元の
> `SampleIME` のまま残してある — それらを改名すると、コードの変更が移動に埋もれて
> 取り込み元との差分が読めなくなるため(決定 0021)。
> `.def` の `LIBRARY` と `.rc` の `VERSIONINFO` も出荷名・出荷者に合わせた
> (それ以前は Microsoft のサンプルを名乗ったままだった)。
> 起動処理は「自分の隣の `OhageyEngine.exe`」を見るので、いずれにせよ DLL の
> 名前には依存しない。

## エンジンの場所は DLL からの相対で解決する

TSF DLL は自分自身のパス(`GetModuleFileNameW`)を取り、同じディレクトリの
`OhageyEngine.exe` を起動する。

レジストリに絶対パスを書く案は採らない。

- **状態が増えない。** レジストリ値とファイル配置が食い違う可能性を作らない。
  インストーラのバグやユーザーのフォルダ移動で「登録されているが存在しない」
  状態になり得る。
- **DLL と exe は同時に配布される。** 別々の場所に置かれる理由が無い以上、
  相対で足りる。
- **読み取り権限の問題が無い。** AppContainer から HKLM を読む話をしなくて済む。

## 起動の作法

- `CreateProcessW`、`CREATE_NO_WINDOW | DETACHED_PROCESS`。
  ホストアプリのコンソールやジョブに紐づけない。エンジンは**起動元より長生きする**
  (決定 0015 のアイドルタイムアウトで自分で終わる)。
- ハンドルは継承させない。ホストアプリのハンドルをエンジンに渡す理由が無い。
- **同時起動は放置してよい。** 複数のアプリが同時に起動を試みても、
  `FILE_FLAG_FIRST_PIPE_INSTANCE`(決定 0031)で負けた側が
  `pipeNameAlreadyOwned` として即座に終了する。排他を自前で持つ必要が無い。

## 起動後に待たない

起動してもパイプはすぐには現れない(エンジンは辞書を読んでから listen する)。

**打鍵スレッドを数秒止めるのは論外**なので、起動を投げたら短時間だけ様子を見て、
繋がらなければ諦めて戻る。次の打鍵で再試行すれば、2〜3打鍵目には繋がる。
最初の1〜2打鍵が変換されないのは、IME が数秒固まるよりはるかにましな失敗。

## 暴走させない

エンジンが起動直後に落ち続ける状態だと、素朴に書けば**打鍵ごとにプロセスを生成する**。
起動の試行には間隔(数秒)を設ける。

## 開発用の上書き `OHAGEY_ENGINE_PATH`

開発中は DLL の隣にエンジンが無い。パスを上書きできるようにするが、
**`OHAGEY_ALLOW_ENGINE_PATH_OVERRIDE` を定義してビルドしたときだけ**有効にする。
DLL のプロジェクトはこれを定義しないので、出荷される DLL にはコードごと存在しない。

決定 0008 の `OHAGEY_MODEL_PATH` より厳しくしているのは、**こちらは任意のコードを
実行させる穴**だから。読むモデルを選ばせるのとは危険度が違う。実行時フラグではなく
コンパイル時に落とす。

## 追記(2026-08-04)— インストーラの `[Files]` を実物と突き合わせた。3件中2件が間違っていた

`iscc` を通す前に、`installer/ohagey.iss` の `[Files]` が指しているパスが**実際の
ビルド成果物と一致するか**を確認した。ずっとコメントアウトされたままだったので、
誰も確かめていなかった。

| 対象 | 書いてあったパス | 実際 |
|---|---|---|
| TSF DLL | `..\tsf\SampleIME\x64\Release\OhageyTSF.dll` | ✅ 正しい(実物を確認) |
| エンジン | `..\engine\.build\release\OhageyEngine.exe` | ❌ **存在しない** |
| 設定アプリ | `..\settings-app\bin\x64\Release\OhageySettings.exe` | ❌ **場所も形も違う** |

### エンジン — `.build\release` はシンボリックリンクで、作成に失敗する

SwiftPM は `.build\release` を**アーキテクチャ三つ組ディレクトリへのシンボリック
リンク**として作る。Windows でシンボリックリンクを作るには開発者モードか昇格が要り、
**普通の環境では失敗する**。この機械でも毎回こう出ている:

```
warning: unable to create symbolic link at <...>\.build\release:
encountered an I/O error (code: 512)
```

そしてディレクトリは残らない。リンクの名前で参照すると、**リンクを作れない機械
——つまり大半——でパッケージングが失敗する**。

`..\engine\.build\x86_64-unknown-windows-msvc\release\OhageyEngine.exe` に直した。

### 設定アプリ — ファイル1つではなくディレクトリ

`Ohagey.Settings.csproj` は `RuntimeIdentifier=win-x64` かつ
`WindowsAppSDKSelfContained=true` なので、出力は**アプリと Windows App Runtime の
一式**(数十ファイル)になる。`OhageySettings.exe` だけを入れると、
**起動できないものをインストールすることになる**。

しかも self-contained にしたのは、まさに「インストール後にランタイムの
ダウンロードを訊きにいかせない」ため(決定 0016)だった。1ファイルだけ入れると
その目的ごと失われる。

パスも違っていた。プロジェクトは `settings-app\src\Ohagey.Settings\` にあり、
TFM と RID がパスに入る:

```
..\settings-app\src\Ohagey.Settings\bin\x64\Release\net8.0-windows10.0.19041.0\win-x64\*
```

`Flags: recursesubdirs createallsubdirs` を付けてディレクトリごと入れるようにした。

### まだ通していない

`iscc` はこの機械に入っていないので、**コンパイルは確認できていない**。
`[Files]` の行はコメントのままである。確かめたのは「パスが実在するか」までで、
「Inno がこれを受け付けるか」はまだ。

## 追記(2026-08-04、その2)— `iscc` を通した。`AppId` が仮のままだった

Inno Setup 6.7.3 を入れて `installer/ohagey.iss` をコンパイルした。**通る。**
`ohagey-setup.exe`(2.00 MB)が出来る。

> `winget install JRSoftware.InnoSetup` は **`%LOCALAPPDATA%\Programs\Inno Setup 6\`**
> に入る。`C:\Program Files (x86)\Inno Setup 6\` を探しても無い。

### `AppId` を確定させた

`AppId={{REPLACE-WITH-GENERATED-GUID}}` のままだった。**Inno はこれをエラーにしない** —
リテラル文字列として受け取り、そのままコンパイルが通る。

`AppId` は Windows が「既存のインストール」を見分けるための識別子である。仮の値のまま
出荷して後で変えると、**更新が更新にならず、2つ目のおはぎーが並んで入る。**
一度きり生成して固定した:

```
AppId={{FA549B8C-7981-4ABE-A7CC-1F7DC99E15E7}
```

(二重の `{` は Inno のエスケープで、リテラルの `{` を意味する。)

`MyAppURL` の `REPLACE_ME` も実際のリポジトリに直した。

### 有効にしたもの / まだコメントのままのもの

**有効:**

- `[Files]` の `download-model.ps1` — リポジトリに実在するファイルで、ビルド成果物では
  ないため
- `[Run]` の6行すべて — zenz の重み1件と base LM の5件。リリース資産が公開されたので
  URL が実在するようになった

**コメントのまま:** TSF DLL / エンジン / 設定アプリ / バックエンド DLL。
いずれもビルド成果物で、CI でそれらを作る段取りが要る。

いまの `ohagey-setup.exe` は「`download-model.ps1` を入れて、モデル5件+重みを
取得する」ところまでを行う。**アプリ本体はまだ入らない。**

### まだやっていない

- [ ] **実際にインストールしてみる。** `%ProgramFiles%\Ohagey\` に書き、80MB を
      取得する。この機械の状態を変えるので指示待ち
- [ ] CI で `iscc` を回す

## 追記(2026-08-04、その3)— 実際にインストールして、出荷構成で通した

`ohagey-setup.exe` を `/VERYSILENT` で実行した(`PrivilegesRequired=admin` なので
UAC の承認が要る)。終了コード 0。

### 入ったもの — 84.1 MB

```
download-model.ps1                0.01 MB
models\ggml-model-Q5_K_M.gguf    70.45 MB
models\lm_c_abc.marisa            3.22 MB
models\lm_c_bc.marisa             1.78 MB
models\lm_r_xbx.marisa            0.77 MB
models\lm_u_abx.marisa            1.75 MB
models\lm_u_xbc.marisa            1.90 MB
models\download.log
unins000.exe                      4.25 MB
```

`download.log` の1行目が効いている:

```
ggml-model-Q5_K_M.gguf : already present and matches -- nothing to do
```

70MB の重みは既にあったので**ハッシュを確認して飛ばした**。再インストールや修復で
毎回落とし直さない、という設計が実物で効くことの確認になった。

### 出荷構成での実測 — **release ビルド、環境変数の上書きなし**

これまでの個人化の測定はすべて debug ビルド + `OHAGEY_BASE_LM_PATH` だった。
インストーラが `%ProgramFiles%\Ohagey\models\` に base を置いたことで、
**初めて出荷そのものの構成で測れる**ようになった。

```
base language model: present (installed, C:\Program Files\Ohagey\models\lm)

=== 学習ストアのみ(個人化オフ)===   服を切る   2位 → 2位
=== 学習ストア + 個人化 ===          服を切る   2位 → 1位
```

評価セット32件: `correct at rank 1: 30 -> 30 of 32 (broke 0, fixed 0)`、`ALL PASSED`。

**debug + 環境変数で測ってきた結果が、release + 実インストールでそのまま出る。**

### 既定オンが実際に効くことの確認

`HKCU\Software\Ohagey` を**丸ごと消して**(=まっさらな利用者と同じ状態)release の
エンジンを起動した:

```
OhageyEngine: settings loaded (learning=true, backend=cpu)
OhageyEngine: personalisation: using the installed base language model
OhageyEngine: personalisation: no trained model yet
OhageyEngine: personalisation: generation 1 published (3 lines, 8818ms)
OhageyEngine: personalisation: deferring the next training run by 79s to stay under 10% of the machine
```

- 設定キーが1つも無い状態で**個人化が動いている** — 既定オンが実際に効いている
- 8,818ms は 9.4MB の base に対する予測(7.9秒)どおり
- **duty cycle も実物で効いている** — 次の学習を79秒遅らせている

### アンインストール

`%ProgramFiles%\Ohagey\unins000.exe`、または「アプリと機能」から。
学習データは `%LOCALAPPDATA%\Ohagey\` にあり、これは消えない(決定 0024 / 0025 —
消すのは設定アプリの「学習データを消去」)。

## 追記(2026-08-04、その4)— 全部入りで組んで登録した。**黙って外れていた2件**

TSF DLL・エンジン・設定アプリを実際にビルドして `[Files]` を有効にし、
`iscc` で 28MB のインストーラを作って入れた。**そこで初めて分かったことが2件ある。
どちらもビルドもインストールも成功したまま外れていた。**

### 1. 🔴 IME が**中国語**として登録されていた

```
HKLM\SOFTWARE\Microsoft\CTF\TIP\{D2291A80-...}\LanguageProfile\
  0x00000804   Description=Sample IME
```

`0x0804` は簡体字中国語である。vendoring 元の SampleIME は**中国語の拼音サンプル**で、
`Define.h` がそのまま残っていた:

```c
#define TEXTSERVICE_LANGID  MAKELANGID(LANG_CHINESE, SUBLANG_CHINESE_SIMPLIFIED)
static const WCHAR TEXTSERVICE_DESC[] = L"Sample IME";
```

**どこもエラーにならない。** ビルドは通り、`regsvr32` は成功し、CLSID はレジストリに
載り、TIP も登録される。ただ**日本語の入力方式一覧には永久に出てこない** —
そこに登録されていないので。

日本語に直した:

```c
#define TEXTSERVICE_LANGID  MAKELANGID(LANG_JAPANESE, SUBLANG_JAPANESE_JAPAN)
static const WCHAR TEXTSERVICE_DESC[] = L"おはぎー";
```

直後の再インストールで `0x00000411  Description=おはぎー` を確認した。

> ⚠️ `DllUnregisterServer` は**コンパイル時の LANGID** で登録解除するので、
> LANGID を変えると前の登録が孤児になる。この機械には
> `0x0804 / Sample IME` が残っている(削除には昇格が要る)。
> 出荷前に変えたので、利用者には起きない。

### 2. 🔴 自前のバイナリが再インストールで**永久に更新されない**

`Define.h` を直して再ビルドし、入れ直したのに、**登録されたのは古い DLL のまま**だった。
インストール先のハッシュが変わっていなかった。

Inno はバージョン資源を比べて「新しくない」ファイルの上書きを飛ばす。
**おはぎーのバイナリはどれもバージョンを上げない** — TSF DLL は vendoring 元の
バージョン資源をそのまま持ち、Swift のエンジンには資源が無く、設定アプリのものも
ビルドで動かない。つまり**最初のインストールが永久に勝つ。**

ログには何も出ない。「上書きを飛ばした」という行が無いまま、そのあとの
`regsvr32` が**古いビルドを登録する。**

自前の成果物すべてに `ignoreversion` を付けた。付けた直後に DLL が置き換わり、
日本語プロファイルが出た。

### GPU バックエンドはパッケージ時に選ぶ

`backends\cuda` は **977MB**(cpu は 2.4MB)で、大半はベンダーランタイムである。
既定で同梱すると、多くの機械で使えない機能のためにインストーラが 1GB を超える。

```
iscc /DGpuBackends installer\ohagey.iss
```

`skipifsourcedoesntexist` も付けてある。`fetch-backends.ps1` は求められたものしか
取ってこないので、define を渡すことが「全部取得済みであること」を要求すべきではない。

### 入ったもの

| | |
|---|---|
| インストーラ | **28.0 MB**(GPU バックエンド無し) |
| 展開後 | **212.2 MB** |

内訳の大半は設定アプリの自己完結 WinUI ランタイム(約 96MB)とモデル(約 80MB)。

### まだやっていない — 実際に打つ

登録はできたが、**日本語の入力方式一覧に追加して切り替える**のは利用者ごとの設定
変更なので、そこは手を出していない。実際にメモ帳で打つのはその先である。

## 追記(2026-08-04、その5)— 実際に打ってもらった。**1打鍵ごとにフル変換していた**

インストールして日本語の入力方式に追加し、メモ帳で打ってもらった。

**打てて、入る。** TSF → 名前付きパイプ → エンジン → 変換 → 確定が実アプリで通った。
エンジンは `C:\Program Files\Ohagey\OhageyEngine.exe` として **メモ帳の中から起動された**
— 決定 0015 のオンデマンド起動が実機で成立している。

そして最初の使用感が2つ返ってきた: **「変換が一回限り」「若干もっさり」**。

### もっさりの原因 — 打鍵ごとの変換

`_HandleCompositionInputWorker`(合成中の文字入力パス)が、キーを1つ処理するたびに
`GetCandidateList` を呼び、それがそのまま `Convert` としてエンジンへ飛んでいた。

これは vendoring 元がピンイン IME だからである。ピンインには**役に立つ中間形が無い**
ので、常に候補一覧から選ぶ。日本語には中間形がある — 画面のかな — そして変換は
利用者が**頼む**ものである。

残しておいたのは様式の問題ではなく、速度の問題だった:

| | |
|---|---|
| 1変換の実測 | **137ms**(release / CPU) |
| `nihongo` と打つ | **7回** |
| 数語打った後のエンジンの CPU | **12.1秒** |

決定 0034 で候補順の振動を直したとき、`ConversionService.convert` は毎回
`stopComposition()` してラティスを作り直すようにした。あのとき「1打鍵ずつ渡す
キーボードには正しく、ここでは間違っている」と書いたのは**エンジン側**の話だったが、
**TSF 側は実際に1打鍵ずつ渡していた。** 両側を見て初めて噛み合っていないと分かる。

`_HandleCompositionInputWorker` から候補取得を外した。かなを見せて、訊かれるまで待つ。
変換は `_HandleCompositionConvert`(変換キー)が行う。

前の変換で開いたままの候補窓は空にする — 読みがその下で変わってしまっており、
出ているのは誰も訊いていない質問への答えだからである。

### 「変換が一回限り」はまだ説明できていない

痕跡だけはある。実セッションのあと `%LOCALAPPDATA%\Ohagey\personal\corpus.txt` が
**存在しなかった** — 確定がエンジンの `record` まで届いていない。
`NotifyCommitted` は**候補一覧から確定した経路でしか呼ばれない**
(`KeyHandler.cpp` の一方の分岐のみ)。ただしこれが原因かは確かめていない。

### 診断できない理由 — **DLL から起動したエンジンのログが残らない**

エンジンは `print` で標準出力に書くだけなので、TSF DLL が起動した実セッションでは
どこにも残らない。このリポジトリが何度も踏んできた「見えない失敗」と同じ形で、
いま実機の診断がそれで止まっている。

- [ ] **エンジンのログをファイルにも書く**(`%LOCALAPPDATA%\Ohagey\engine.log`)。
      実機での診断はこれが無いと始まらない
- [ ] ログが取れたら「一回限り」を再現して原因を特定する

## 追記(2026-08-04、その6)— 登録済みの TSF DLL は置き換えられない。**再起動が要る**

打鍵ごとの変換を直した DLL を入れ直そうとして、**インストールが3回失敗した。**

```
DeleteFile: The existing file appears to be in use (5). Retrying.
```

TSF DLL は登録されると、**テキスト入力面を持つプロセスすべてに読み込まれる。**
実際に押さえていたのは:

```
RestartManager found an application using one of our files: SearchHost.exe
RestartManager found an application using one of our files: Claude
RestartManager found an application using one of our files: Discord
RestartManager found an application using one of our files: Intel(R) Killer(TM) Performance Suite
RestartManager found an application using one of our files: LINE
```

5つ。**どれもおはぎーとは無関係のアプリ**である。IME を更新するために、
利用者のチャットクライアントを閉じることになる。

Inno の既定は RestartManager で**それらを閉じる**ことだった。黙って選んでよい取引では
ない(サイレントインストールでは確認も出せないので、そのまま cancel していた)。

`restartreplace` と `uninsrestartdelete` を付けた。ロック中のファイルは `MoveFileEx`
で予約され、**次のサインインで入れ替わる**:

```
DeleteFile: The existing file appears to be in use (5). Will replace on restart.
Delaying registration of all files until the next logon since a restart is needed.
Need to restart Windows? Yes
```

**IME のインストーラがサインアウトを求めるのはこれが理由である。** 避けようがない —
更新したいまさにその DLL を、他人のプロセスが握っている。

### 帰結

- **エンジンと設定アプリは即座に入れ替わる**(誰も握っていない)。
  TSF DLL だけがサインインを待つ
- つまり**更新直後は、新しいエンジンと古い DLL が組み合わさる**瞬間がある。
  ワイヤ互換(決定 0007)はこれを支える必要がある — 世代をまたいで壊れない形で
  あることが、ここで初めて実際に要求される
- サイレント更新するなら `/NOCLOSEAPPLICATIONS` を必ず渡すこと。
  渡さないと Inno が利用者のアプリを閉じにいく

## 追記(2026-08-04、その7)— LANGID を直しても、**中身は中国語のままだった**

その4で `TEXTSERVICE_LANGID` を日本語に直し、日本語の入力方式一覧に出るようになった。
それで終わりではなかった。vendoring 元は**簡体字中国語の拼音 IME** であり、
言語に紐づく設定が他にも残っていた。**8か所。**

### 利用者に見えていたもの

| | 直した内容 |
|---|---|
| 候補ウィンドウのフォント | `Microsoft YaHei UI` → **`Yu Gothic UI`** |
| タッチキーボード | `TKBL_OPT_SIMPLIFIED_CHINESE_PINYIN` → **`TKBL_OPT_JAPANESE_ABC`** |
| 言語バー(3つ) | `Chinese/English input (Shift)` ほか → **かな/英数の切り替え、全角/半角、句読点** |
| 言語バーの分類名(3つ) | `Conversion mode` / `Character width` / `Punctuation` → **入力モード / 文字幅 / 句読点** |

**フォントは見た目の好みの問題ではない。** CJK は統合されているので、同じコードポイントの
漢字が地域ごとに違う字形で描かれる。簡体字中国語のフォントで日本語を出すと、
**字は出るが形が日本語ではない**。そして `CreateFont` はそれを一切報告しない —
その文字に対して不適切な書体を要求しても、成功して返る。

**タッチキーボードは拼音配列を出していた。** かなも変換キーも無いので、
キーボードがあれば何ともないまま、**タブレットでは使えない**という形で外れていた。

### `#pragma code_page(1252)` — 日本語を入れる前に踏むところだった

`.rc` の文字列は 1252 のままだった。ここに日本語を書くと **rc.exe は何も言わずに
バイト単位で符号化し**、言語バーには文字化けが出る。65001 に変えて UTF-8 で保存した。
`.cpp` 側は `/utf-8` が指定済みなので、そちらはそのままでよい
(`おはぎー` が正しく登録されたのはこれのおかげである)。

**ビルド結果の DLL から実際に読み出して確認した** — 文字列が UTF-16 で正しく入っていること、
`Microsoft YaHei UI` と `Chinese Simplified QuanPin` が消えていること。

### 死んでいた拼音辞書の読み込みを外した

`SetupDictionaryFile()` が起動のたびに `SampleIMESimplifiedQuanPin.txt` を開こうとしていた。
出荷していないので必ず失敗し、作られた `CTableDictionaryEngine` は**どこからも参照されない** —
検索はエンジンがやる。関数ごと外した。

### 教訓

**`LANGID` は入口でしかない。** 言語を差し替えるということは、フォント・入力配列・
既定の文字幅・キーの説明といった、**言語に紐づくものを全部見る**ということである。
どれも「間違っている」とは言ってくれない。ビルドは通り、登録は成功し、
**日本語として少しずつ違うものが動く**。

## 追記(2026-08-04、その8)— エンジンのログをファイルに書く

実機で「変換が一回限り」という報告をもらったが、**追えなかった。**

エンジンは TSF DLL がメモ帳や Chrome の**中から**起動する(決定 0015)。その
プロセスにはコンソールが無いので、`print` した行はどこにも残らない。
**実際に使ったセッションが、自分について何も言い残さない。**
これまで実機で見つかった問題は、全部この状態で診断してきた。

`%LOCALAPPDATA%\Ohagey\engine.log` に書くようにした。実際に出た:

```
2026-08-13 07:35:06.942 [6916] starting: ...\OhageyEngine.exe
2026-08-13 07:35:06.947 [6916] settings loaded (learning=true, backend=cpu)
2026-08-13 07:35:07.024 [6916] pipe name: \.\pipe\ohagey_session_1
2026-08-13 07:35:07.064 [6916] personalisation: using the installed base language model
2026-08-13 07:35:07.073 [6916] listening
```

pid を入れてあるのは、**1日に何度もエンジンが起動して終わる**(決定 0015)ため。
全部が同じファイルに追記するので、pid が無いと誰の行か分からない。
`starting:` で実行ファイルの場所を出すのは、**いま動いているのがどのビルドか**を
機械の中から答えられるようにするためである — インストーラが古い DLL を登録し直して
いたとき(その4)、これが分からなかった。

### 打った内容は絶対に書かない

読みも候補も確定文字列も入れない。入れれば **その機械で書かれた文章の平文の記録**が、
誰も存在を覚えていないファイルとして残る。診断のために開けた穴としては、
元の問題よりはるかに悪い。いまエンジンが出している行は件数・状態・エラーコードだけで、
その線を守る。個人化のコーパスは**別の、意図された、利用者が消せる**保管である
(決定 0024 / 0025)。これはそれではない。

1MiB で1世代だけ回す。エンジンは1日に何度も起動して追記するので、上限が無いと
誰も見ていない機械で永久に育つ。

### 🔴 `FileHandle.write(_:)` は**失敗するとプロセスを落とす**

書いたテストの1つ(`testGivesUpQuietlyWhenTheFileCannotBeOpened`)が
**テスト実行ごとクラッシュさせた。**

```
Foundation/FileHandle.swift:709: Fatal error: 'try!' expression unexpectedly raised an error
```

`try` の付かない `FileHandle.write(_:)` は、中身が `try!` である。
つまり書き込みが失敗すると **`fatalError` に落ちる**。

このクラスは全体が best effort で書いてある — ログが取れないことは診断を1つ失う
だけで、そのために変換を止めるのは間違っている、という理由で。**その前提が逆さまに
なっていた**: 最悪の場合が「ログが無い」ではなく「**打っている最中に IME が死ぬ**」に
なる。ディスクが埋まる、ハンドルが下から無効化される、それだけで届く。

`try handle.write(contentsOf:)` に変え、失敗したら諦めるようにした。
併せて、**ログの場所がディレクトリだった場合**を open で弾く — Windows では
ディレクトリを書き込みで開くのが**成功する**ので、最初の write まで失敗が出てこない。

**テストが無ければ出荷まで残っていた。** そしてこれが落とすのは、
利用者が文字を打っているプロセスである。

## 追記(2026-08-04、その9)— 「変換が一回限り」の原因。**候補窓の有無をポインタで判定していた**

`_HandleCompositionInput` の先頭にこれがある:

```cpp
if ((_pCandidateListUIPresenter != nullptr) && (_candidateMode != CANDIDATE_INCREMENTAL))
{
    _HandleCompositionFinalize(ec, pContext, FALSE);
}
```

訊きたいことは「**候補窓が出ている最中に文字が打たれたか**」である。出ているなら、
新しい語を始める前にいま出ているものを確定する。

**ポインタはその質問に答えられない。** `_DeleteCandidateList` は
`_EndCandidateList()` を呼んで `_candidateMode = CANDIDATE_NONE` にするが、
**`_pCandidateListUIPresenter` を null にしない**。確定・取り消し・合成終了、
どの経路も通る後始末がそれである。

結果、**一度でも変換すると、以降すべての打鍵でこの条件が真になる。**

| 打鍵 | 起きること |
|---|---|
| 確定した直後 | presenter は残る、mode は NONE |
| 次の語の1文字目 | finalize が走る。まだ合成中でないので実害なし |
| **2文字目** | finalize が走り、**合成が終わって1文字目だけが単独で確定**。`_HandleCancel` が読みバッファを消す |

**読みが二度と1文字を超えられない。** だから変換は二度と起きない。
報告された「変換が一回限り」はこれである。

### これは私が入れたものではない — **隠していたものを外しただけ**

その5で打鍵ごとの変換をやめたとき、`_CreateAndStartCandidate` の呼び出し元が消えた。
あれは打鍵のたびに `_candidateMode = CANDIDATE_INCREMENTAL` を立てていたので、
**エンジンが候補を返しさえすればこの条件が偽になっていた。**
バグではなく**マスク**が消えた。元から壊れていた。

### 直し方 — presenter を消すのではなく、mode で判定する

`_DeleteCandidateList` で `delete` するほうが筋に見えるが、そこは
プロファイル無効化からも合成の後始末からも呼ばれ、対象は参照カウント付きで
TSF が握っている可能性がある。**この DLL は利用者のアプリの中で動く**(決定 0017)。
条件を `_candidateMode` に寄せた — 候補窓が出ているかを知っているのは元々そちらである。

### 学び

**同じ事実を2つの場所で持つと、片方だけ更新される。** ここでは「候補窓が出ているか」を
ポインタと mode の両方が持っていて、後始末は mode しか直さなかった。
そして**間違ったほうを読んでいた**。

## 追記(2026-08-04、その10)— ログを取ったら、**ログのほうに3つ問題があった**

「変換が一回限り」を実機で追うためにログを入れた。最初の実セッションのログを読んで、
**追跡には使えないことが分かった。**

### 1. リクエストが1行も残らない

起動と設定しか書いていなかった。実セッションのログ全体がこれである:

```
17:00:49 [22884] listening
17:01:07 [22884] Zenzai model loaded
```

**変換が何回届いたのかが分からない。** 「一回限り」は届いたリクエストの数と形に
ついての主張なのに、その両方が残らない。

`RequestRouter` で1件ごとに書くようにした。デコード済みのリクエストと応答の
両方を見る唯一の場所である。

```
#3 convert (reading 5, preceding 12, n_best 0) -> 9 candidates (zenzai true) in 148ms
#4 commit (reading 5, text 3, learn true) -> ok in 2ms
```

**長さだけで、中身は書かない。** そして長さが要点である — 疑っているのは
「一度変換すると読みが二度と1文字を超えられない」ことなので、`reading 1` が
並ぶかどうかが答えになる。語そのものは何も足さない。
テストで中身が漏れないことを固定した。

### 2. 🔴 タイムスタンプが **UTC なのに local と書いてあった**

実セッションのログは `08:00:49`。**打っていたのは 17:00 である。**
ゾーンを表示しない形式なので、**そのまま local に見える。**私自身、
最初これを 08:00 の出来事として読んだ。

原因は Foundation ではなく Swift for Windows:

```
TimeZone.current: GMT 0
autoupdating   : GMT
forced Tokyo   : 2026-08-13 17:14:22   ← ゾーンDBはある
Date()         : 2026-08-13 08:14:22 +0000
```

**`TimeZone.current` が GMT を返す。** ゾーンのデータベースはあり、
`Asia/Tokyo` を明示すれば正しく出る。**「いまのゾーン」の取得だけができない。**

`OhageyEngineCore` は WinSDK に依存しない規約なので、ゾーンは引数にした。
`OhageyEngine` 側が `GetTimeZoneInformation` で Windows に訊いて渡す。
入れ替えて `17:19` を確認した。

**時計が9時間ずれた診断ログは、無いより悪い。** 利用者の「その時こうなった」と
突き合わせるためのものが、突き合わせを壊す。

### 3. 🔴 インストーラが**自分のエンジンを止めない** → 更新が丸ごとロールバック

```
DeleteFile: The existing file appears to be in use (5). Retrying.
User canceled the installation process. Rolling back changes.
```

**エンジンが動いていると更新できない。** そしてこれは稀ではない —
TSF クライアントはパイプ接続を張りっぱなしにするので**アイドルタイムアウトが
発火せず**、誰かがログイン後に一度でも打っていれば動いている。

`PrepareToInstall` で `OhageyEngine.exe` だけを止めるようにした。
**Inno の `CloseApplications` は使わない** — RestartManager が見つけるのは
Discord や LINE に読み込まれた TSF DLL のほうで、IME の更新のために
利用者のチャットクライアントを閉じることになる(その6)。止めるのは1つ、
そしてそれは自分のプロセスである。持っている状態は全部ディスクにあり、
次の変換で起動し直す(決定 0015)。

## 追記(2026-08-04、その11)— 「中国語判定になる」の正体は**アイコンだった**

タスクバーの表示が中国語になる、という報告。レジストリを疑って全部見た —
`HKLM` の TIP は `0x00000411 / おはぎー` の1つだけ、利用者側の入力方式一覧も
`ja => 0411:{D2291A80-…}`、`SortOrder\AssemblyItem` も `0x00000411` のみ、
`Preload` は `00000411` と `00000409`。**中国語はどこにも無かった。**

アイコンを画像として出力して見た:

| ファイル | 中身 |
|---|---|
| `ImeModeOn.ico` | **中** |
| `ImeModeOff.ico` | **英** |
| `SampleIme.ico` | **样** |

**装飾ではなく文字だった。** `中`/`英` は中国語入力の指示器そのもの、
`样` は簡体字にしか無い字形である。選ぶと**タスクバーに中国語の文字が出る。**

**登録は何も間違っていなかった。絵が中国語だと言っていた。**

`tsf/Ohagey/tools/make-icons.ps1` で作り直した — **あ**(かな入力) / **A**(英数) /
**お**(おはぎー本体)。日本語の IME が並んで出す表示に揃えた。
`全`/`半` と句読点は日本語でも同じなので触っていない。

### 教訓

決定 0021 は「LANGID だけでは足りない」と書き、その7で設定を8か所直した。
**それでもまだ足りなかった** — 見落としていたのは**コードではなくデータ**である。
`.ico` の中身は grep に掛からない。「中国語のものが残っていないか」を
**画像まで含めて**確認しないと、この種は残る。

## 追記(2026-08-04、その12)— リクエストログが答えを出した。**「一回限り」は2回目のスペースだった**

その10で入れたリクエストログに、実セッションの中身が出た:

```
17:56:06 #1 convert (reading 5, preceding 0, n_best 9) -> 9 candidates (zenzai true) in 222ms
17:56:09 #2 convert (reading 4, preceding 0, n_best 9) -> 9 candidates (zenzai true) in 246ms
17:56:13 #3 convert (reading 3, preceding 0, n_best 9) -> 9 candidates (zenzai true) in 154ms
```

**エンジンは最初から正しく答えていた。** 変換は3回届き、読みは5文字・4文字・3文字、
どれも9候補を返している。**その9の仮説(読みが1文字を超えられない)は外れである。**

### 本当の原因 — 2回目のスペースが「次の候補」ではなく**確定**

候補窓が出ている状態でのスペースは `CATEGORY_CANDIDATE` の `FUNCTION_CONVERT` に
行き、`_HandleCandidateConvert` が呼ばれる。これは選択中の候補から**フレーズを作る**
処理 — ピンインの動作であり、しかもエンジンが置き換えた表辞書に依存していた。
作れるフレーズが無いので `else` に落ち、**`_HandleCandidateFinalize` に行く。**

つまり:

| | |
|---|---|
| スペース1回目 | 変換。候補窓が出て1番目が選択される |
| スペース2回目 | **1番目を確定する**(2番目に行かない) |

キーボードから見れば、これはまさに**一回限り**である。リストの2番目以降に
永久に手が届かない。

矢印キーと同じ `FUNCTION_MOVE_DOWN` に繋いだ。候補窓の選択・ページ送り・
折り返しの実装は1つであるべきである。

### 🔴 ついでに分かったこと — **確定がエンジンに一度も届いていなかった**

上のログに `commit` が**1行も無い**。

`NotifyCommitted` は `_HandleCompositionFinalize` にしか繋がっていなかった。
しかし利用者が実際に通る道は**変換 → 選ぶ → Enter** であり、そちらは
`_HandleCandidateFinalize` である。**普通に打っている限り学習は一度も呼ばれない。**

決定 0034 で個人化を測り、決定 0024/0025 で学習を配線し、`personal/corpus.txt` が
実セッション後に存在しないことを不思議に思っていた。**理由はこれだった。**
呼ぶ場所を間違えていたのではなく、**利用者が通る側に付けていなかった。**

`_HandleComplete` の前に呼ぶ — 読みの元になるキーストロークバッファを
あれが消すので、順序は他方と同じ制約になる。

### `preceding 0` — 左文脈も届いていない

決定 0034 の追記で `preceding_text` をエンジンに通したが、**TSF 側が常に空を
送っている。** 「同じ読みが文脈で割れるのは5件中4件」という測定は harness の
値であって、実機では効いていない。次に取る。

### 教訓

**両端を見ないと分からない。** エンジン側のログだけを見れば「変換は3回とも成功」、
TSF 側のコードだけを見れば「スペースは FUNCTION_CONVERT に繋がっている」。
噛み合っていないことは、**片方の記録をもう片方の期待と突き合わせて初めて**見える。
その5でも同じ形を踏んでいる(エンジンは1打鍵ずつ渡される前提で直され、
TSF は実際に1打鍵ずつ渡していた)。

## 追記(2026-08-04、その13)— 本体のアイコンは**おはぎの絵**にした

その11で `样` を `お` に置き換えたが、頭文字である以上の根拠は無かった。
絵にした。**モード表示が隣で `あ`/`A` を出しているところに、もう1つ文字を
置く必要は無い。**

輪郭だけで描いている。16px で読まれるものなので、内側の描き込みは灰色の
にじみになる。**残るのは輪郭だけなので、設計を全部輪郭に置いた** —
粒あんの塊が皿に乗っている形である。皿が無いと丘か石に見える。

3回作り直した。記録しておく:

| 作り | 見えたもの |
|---|---|
| 弧を端どうしで繋ぐ | 谷が膨らみと同じ深さになり、**塔が3本の城** |
| 円を重ねて下に矩形 | 谷は直ったが側面が垂直で角が四角く、**パン** |
| 浮いた楕円 | **石**。粒が小さすぎて輪郭を破れず飲み込まれた |
| ドーム + 粒 | ○ |

**16px では粒を描かない。** 2px 未満の膨らみは質感ではなく**へこみ**に見え、
全体が帽子になる。小さいサイズはドームだけ、大きいサイズに粒を入れる。
アイコンを縮めるほど単純化するのは定石で、ここでは**菓子に見えるか
染みに見えるか**の差になっている。

## 追記(2026-08-04、その14)— 実際に使ってもらって出た2件

### 1. 記号の対応表がまだ中国語だった

「半角全角記号がちゃんと打てない」。表を見たらそのままだった:

| 打った键 | 出ていた字 | |
|---|---|---|
| `,` | U+FF0C | 中国語のコンマ。日本語は U+3001 |
| `\` | U+3001 | **中国語 IME は読点をバックスラッシュに置く** |
| `$` | U+FFE5 | 通貨記号 |
| `&` `_` | U+2014 | ダッシュ |
| `@` | U+00B7 | 中黒 |
| `^` | U+2026 | 三点リーダ |

**`$` を打つと通貨記号が、`&` を打つとダッシュが出ていた。** この表は読みになる前の
キーに適用されるので、ここにあるものがそのまま出る。

日本語の5つに置き換えた — `,`→、 `.`→。 `/`→・ `[`→「 `]`→」。
**残りの ASCII 記号はそのまま文書に届く。** 全角にするのは倍角トグルの仕事であって、
利用者が選ぶモードである。句読点の表が黙ってやることではない。

`"`→“” / `'`→‘’(西欧の引用符)と `<`→《〈(中国語の書名号)も外した。
機構は残してあるが、日本語には入れるものが無い。

**`RomajiToKana` は最初から `,` `.` `/` を日本語で持っていた。**
表のほうが先に横取りしていた。

### 2. ダークモードで あ/A が見えない

**Windows はテキストサービスのアイコンを反転しない。** 黒い字は暗いタスクバーの上で
黒いままである。言語バーのボタンに「単色、色は任せる」形式は無いので、
**両方出荷して選ぶ**しかない。

白い版を作り(`make-icons.ps1 -Light`)、`GetIconImpl` が表示のたびに
`SystemUsesLightTheme` を読んで選ぶ。**活性化時にキャッシュしない** —
利用者がテーマを変えたら次の描画で追随する。キャッシュすると次のサインインまで
古いままになり、数時間前に変えた設定と結びつけられる人はいない。

`AppsUseLightTheme` ではなく `SystemUsesLightTheme` である。別々に設定でき、
**アイコンが乗っているタスクバーの色を決めるのは後者**である。
値が無い場合(書き込まれない管理イメージがある)は light と見なす —
Windows の既定であり、明るいバーに黒は正しいが、白は見えない。

幅と句読点のボタンには白い版が無い。切り離した言語バーにしか出ないので、
見づらいことより読み込みに失敗するほうが大きい問題である。

## 追記(2026-08-04、その15)— 日本語配列に合わせる。**キーボードの刻印が全部飾りだった**

ダークモードは直った。残り2件はどちらも「JIS キーボードのキーが仕事をしていない」。

### 1. 半角/全角 も 英数 も効かず、**Shift が切り替えていた**

サンプルの入力モード切り替えは **Shift の単独押し**である(簡体字中国語の作法)。
日本語キーボードでは二重に間違っている:

- **キートップに印刷されている 半角/全角 と 英数 が何もしない**
- **大文字を打つために誰もが押す Shift がモードを飛ばす**

日本語のキーに繋ぎ替えた:

| キー | 仮想キー |
|---|---|
| 半角/全角 | `VK_KANJI`、加えて `VK_OEM_AUTO` / `VK_OEM_ENLW`(ドライバによって別の値で来る) |
| 英数 | `VK_CAPITAL`(修飾なし) |

JIS では **英数 が主、CapsLock が Shift 側**なので、修飾なしに割り当てるのが刻印どおりで、
Shift+CapsLock は CapsLock のまま残る。

`InitPreservedKey` は元から `TSFPreservedKeyTable` を走査していた —
複数のキーが1つの GUID を共有できる。サンプルが1つしか要らなかっただけである。

### 2. 記号 — **一度中国語で、次は削りすぎた**

その14 で中国語の表を句読点5つに置き換えた。**今度は逆に振れていた**:
かな入力中の日本語 IME は記号も全角にするので、`!` が半角のまま出て、
隣の IME が `！` を出すのと食い違った。

JIS 106/109 の表(31件)にした。Microsoft IME の既定に合わせてある:

- **単に広げないもの** — `,` `.` `/` `[` `]` → **、。・「」**
- **バックスラッシュ → ￥**(キーにそう書いてある)
- **`"` `'` → ” ’**、**`~` → 〜**(全角チルダ U+FF5E ではなく波ダッシュ U+301C)
- 残りは U+FF00 ブロックの相方

**英字と数字は入れていない** — 英字は読みそのもの、数字は半角のままが
Microsoft IME の挙動であり、番号を打つ人が欲しいものである。

**IME が開いているときだけ適用する。** サンプルは句読点コンパートメントさえ
オンなら IME の開閉と無関係に効かせていた(中国語 IME は句読点幅を独立した
モードとして扱う)。日本語では**直接入力で `!` が `！` になっていた**。

### 3. `ー` が打てなかった — キーストローク表が **A〜Z の26個だけ**

ピンイン IME に必要なのは26文字だけである。日本語にはもう1つ要る:
**コーヒー や データ の読みには ー があり、それはマイナスキーで打つ。**

表に無いのでマイナスキーは読みバッファに届かず、句読点の経路に落ちて
**合成を確定させてから別の文字を隣に置いていた**。つまり**それらの語は打てなかった。**

`RomajiToKana` は最初から `-` → `ー` を持っていた。**誰も食わせていなかった。**

### 教訓

その7で「LANGID は入口でしかない」と書き、その11で「見落としていたのは
データだった」と書いた。今度は**キーボードそのもの**だった。
言語を差し替えるとは、**利用者の指が触れるものを全部見直す**ことである。

## 追記(2026-08-04、その16)— **IME が閉じているときは何もしない**、が抜けていた

その15 のあと打ってもらった結果:

| | |
|---|---|
| `ko-hi-` → コーヒー | ✅ |
| Shift でモードが変わらない | ✅ |
| `!` `@` `^` がかな入力中も半角 | ❌ |
| 英数(CapsLock)で あ ↔ A が切り替わらない | ❌ |
| **A のときも英語が全角** | ❌(新規) |

### 記号 — 表は正しかった。**誰も読んでいなかった**

その15 で JIS の表を入れたのに効かない。条件が2つあり、**逆方向に両方間違っていた。**

サンプルは**句読点コンパートメントさえオンなら** IME の開閉と無関係に効かせていた
(中国語 IME は句読点の幅を独立したモードとして扱う)。その15 で `isOpen` を足して
直したが、**コンパートメントが必要という条件は残っていた。オフだと表ごと死ぬ。**

日本語にそんなモードは無い。Microsoft IME に「かな入力だが記号は半角」という設定は
無く、半角記号が欲しければ**かな入力から出る** — それは `isOpen` が既に表している。
コンパートメントは見ないことにした。**かな入力中は常に広げる。**

### 「A のときも英語が全角」が決定打だった

閉じているのに変換が起きるということは、**閉じたときに止まる仕組みが無い**ということである。

```c
// サンプル
if (!isOpen && !isDoubleSingleByte && !isPunctuation) return ...;
```

**幅か句読点のどちらかが立っていれば、閉じていても先へ進む。** だから、いつか
Shift+Space(大文字を打とうとして当たりやすい)で幅トグルが入っていると、
**A に切り替えたあとも全角に変換し続ける。**

```c
// いま
if (!isOpen) return ...;
```

**直接入力とは、キーが素通りすることである。** 下にある変換は全部かな入力のもので、
例外は上で処理済みのタッチキーボードのページキーだけである。

この1行が、記号の件も含めて**同じ原因の3つの症状**をまとめて説明していた。

### 英数キー — 家族ごと登録した

`VK_CAPITAL` 単独では届かなかった。JIS の刻印が来うる値をまとめて登録した:
`VK_OEM_ATTN`(0xF0、`VK_DBE_ALPHANUMERIC` — IME が有効なときに 英数 キーが名乗る値。
おそらくこれが欠けていた)と `VK_CAPITAL` を、それぞれ修飾なしと Shift 付きで。
届かないキーは発火しないだけなので、複数登録する費用は表1行ずつである。

## 追記(2026-08-04、その17)— 左文脈を実機で渡す

決定 0034 で `preceding_text` をエンジンに通し、「同じ読みが文脈で割れるのは5件中4件」
まで測った。**実機では一度も届いていなかった** — リクエストログが毎回 `preceding 0`
である。ワイヤにもエンジンにも実装があり、**TSF 側が空文字列を送っていた。**

`_GetPrecedingText` を足した。合成の範囲を複製して先頭に畳み、`ShiftStart` で
30文字ぶん戻してから `GetText` する。文書の先頭付近では `ShiftStart` が短く動くだけで、
`shifted` がそれを教えてくれる。

**毎回読み直す。**追跡して持ち回らない — 利用者は変換と変換の間に別の場所を
クリックし、カーソルの後ろを編集し、文書を切り替える。持ち回った写しは、
そのとき**間違った場所についての自信のある答え**になる。

**最後の改行で切る。**文脈とは、いま書いている文が乗っているもののことで、
上の段落は別の文である。渡せば、利用者が既に終えたものの続きをモデルに書かせることになる。

学習と違い、**壊す対象が無い**。Zenzai は `leftSideContext` として読むだけで、
何も覚えない(決定 0034)。

`SearchCandidateProvider` の呼び出しには空を渡す。あれは別のテキストサービスが
文字列だけを持って呼ぶ逆変換の提供口で、**左に何も無い**。

## 追記(2026-08-04、その18)— 左文脈が実機で届いた。**そして3回、測る対象を間違えていた**

```
tsf.log    17:51:53 [30696] preceding: shift 0x00000000 moved -2, GetText 0x00000000 fetched 2
engine.log 17:51:53 #3 convert (reading 2, preceding 2, n_best 9) -> 9 candidates
```

**届いている。** 決定 0034 でエンジンに通し、決定 0007 からワイヤにあった経路が、
実機で初めて端から端まで繋がった。

### 3回とも「変わっていない」の原因は同じだった

| 回 | 私の説明 | 実際 |
|---|---|---|
| 1 | 「実装の問題かもしれない」 | **インストールが exit 2 で失敗していた**。動いていたのは前日のビルド |
| 2 | 「符号の決め打ちが原因」 | **古い窓で打たれていた**。改名前の DLL を掴んだ Notepad |
| 3 | 「窓を指定したので今度こそ」 | **また古い窓**。私が開いた窓は一度も使われていなかった |

**どれも実装の問題ではなかった。3回とも「そのコードは走っていない」だった。**

そして私は2回目に「符号の決め打ちが原因で、実セッションで毎回 `preceding 0` に
なっていた」と書いた。**これは誤りである** — ログは `moved -2 / -4 / -7` と
**負で返している**ので、元の `shifted < 0` でも通っていた。ガードは外したままにするが
(インターフェースは符号を約束していない)、**あれは原因ではなかった。**
コード中のコメントも直した。

### 効いたのは TSF 側のログだった

`tsf.log` が**存在しないこと自体**が答えだった — 新しいコードは一度も走っていない、と。
中身を読む前に分かった。

エンジンのログを入れたときと同じ結論に、同じ形で二度到達している(その10):
**実機で起きることは、記録が無ければ追えない。** 違いは、今回は
「記録が無い」ことが直接そのまま証拠になった点である。

### 手順として残すこと

**「入っているか」と「走っているか」は別である。** 確認を頼む前に:

1. `Get-FileHash` で**インストール済みとビルドが一致**することを確かめる
2. 読み込み中の DLL は**改名なら通る**(`swap-dll.ps1`)。再起動を待たずに入れ替えられる
3. **スワップ後に起動したプロセスだけが新しいコードを持つ。**
   `Get-Process | ... Modules` で **IME を実際に読み込んでいる窓**を特定する —
   モジュールのパス表示は改名後も同じなので、**起動時刻でしか見分けられない**

### 行が示した設計の確認

```
moved -4, fetched 4 -> returning 0 chars
moved -7, fetched 7 -> returning 3 chars
```

改行で切る処理が働いている。行頭では左文脈が無くなり(4→0)、行の途中では
その行のぶんだけ残る(7→3)。**意図どおりである。**

### 残件

`tsf.log` は変換のたびに2行書く。開発中はこれでよいが、**出荷前にスイッチを付ける** —
利用者の入力経路で常時ファイルを開き続ける理由は無い。

## 追記(2026-08-04、その19)— TSF 側のログは既定で切る

`tsf.log` は変換のたびに2行書き、そのために**ファイルを2回開く**。
場所が悪い — **利用者のアプリの中、入力経路の上**である。
追いかけている間は正しい代償だが、それ以外のときに払う理由は無い。
**出荷された IME が、いつか誰かが HRESULT を見たかったという理由で
スペースキーのたびにディスクに触るのはおかしい。**

`HKCU\Software\Ohagey\DiagnosticLog`(DWORD、非ゼロで有効)を見る。既定は無し = 切。

**一度読んで覚える。** 1行ごとにレジストリを引くのは、書き込みより高くつく。
つまり**有効化はその後に起動したアプリに効く** — ファイルが空のままのときは
これを疑うこと。**DLL の差し替えと同じ罠**である。

キー名は `SettingsSchema.swift` の `Key` にも足した。Swift 側は読まないが、
**あの列挙がレジストリの契約**であり、C++ 側にしか無い名前は誰かが再利用する名前である。
設定アプリがスイッチを出せる必要もある。

### エンジンのログは切らない

同じ扱いにしていない。エンジンは**自分のプロセス**で動き、1リクエスト1行で、
**実機の診断を可能にしたのはあれそのもの**である。1MiB で回るので上限もある。
入力経路の上にいるかどうかが、両者の違いである。

## 追記(2026-08-04、その20)— 無変換で確定できない。**同じ根が3つ目の症状を出した**

「変換せずに Enter で確定できない」。かなが**入らずに消える**。

`_HandleCompositionFinalize` の分岐がまた**ポインタで候補窓の有無を判定**していた:

```cpp
if (isCandidateList && _pCandidateListUIPresenter)   // 候補確定の道
else                                                  // 合成テキストをそのまま確定
```

`CKeyStateComposing::HandleKeyFinalizeCandidatelist` は `isCandidateList = TRUE` で
呼ぶ。**候補窓が出ていなくても**である。そして `_pCandidateListUIPresenter` は
一度変換すると二度と null に戻らない(`_DeleteCandidateList` が null にしない)。

結果:

| | |
|---|---|
| 最初の変換をするまで | ポインタが null なので else に行き、**かなが入る** |
| 一度でも変換した後 | 候補側に行き、終了済みリストに選択を訊いて空が返り、
何も確定しないまま `_HandleCancel` が走って**かなが捨てられる** |

**追記9(変換が一回限り)と追記20 は同じ1つのバグの別の症状である。**
どちらも「候補窓が出ているか」をポインタに訊いていた。3か所目が無いか、
`_pCandidateListUIPresenter` を読んでいる箇所を全部見直すこと。

`_candidateMode != CANDIDATE_NONE` を足した。

### 残す問い — 無変換確定を学習に送るか

いまは送っていない。`NotifyCommitted` は候補を確定した経路にしかない。
読み = 表記の対になるので変換の順位には効かないが、**コーパスの1行としては
利用者が書いた文そのもの**であり、個人化の n-gram には効く。
効かせると「無変換で確定した語のかな表記が候補として上がる」副作用が付く
(Microsoft IME はそう振る舞う)。**測ってから決める。**

### 無変換確定も学習に送る(同日、決定済み)

保留にしていた問いに答えを入れた。**送る。**

理由は単純で、**無変換で確定した文字列は利用者が書いた文そのもの**だからである。
個人化のコーパスは「その人が書くもの」を学ぶためにあり、変換を経たかどうかは
その性質を変えない。決定 0034 で測ったとおり、昇格に効くのはコーパスの中身であって、
経路ではない。

学習ストアに入る対は**読み = 表記**になる。変換の順位は動かせない(同じものを
同じものに写像するだけ)が、**そのかな表記自体がその読みの候補として上がる。**
Microsoft IME は同じキーストロークのあとに同じ振る舞いをする。

実装上の制約が2つ、他の2か所と同じ形で出る:

- **`_EndComposition` の前に読む** — 終わったあとに範囲は無い
- **`_HandleCancel` の前に呼ぶ** — あれが読みの元のキーストロークバッファを消す

セキュアモードでは `updateLearning` を落とす。ログオン画面や UAC で打ったものを
覚えるのは、利用者の設定が何であれ誤りである(3か所目にして同じ規則)。

## 追記(2026-08-04、その21)— **どのビルドが動いているかを、DLL 自身に言わせる**

一日で**6回**、同じ形で間違えた:

| | 何が起きたか |
|---|---|
| ×2 | ビルドしたものが**インストールされていなかった**(exit 2 で失敗 / release を組み忘れ) |
| ×3 | **古い窓**で試されていた。改名前の DLL を掴んだプロセス |
| ×1 | 合成キー入力が**別の窓に入った**。前面化に失敗し、利用者のチャットに `aiueo` が3回送られた |

**どれも「修正が効かない」に見えた。** そして毎回、解決は「プロセスの起動時刻と
差し替え時刻を人間に突き合わせてもらう」だった。**それを頼み続けたのが誤り**である。

読み込み中の DLL は置き換えられないので、**動いているアプリはそれぞれ違うおはぎーを
持ちうる**。そして画面にはどこにもそれが出ない。

`Ohagey::LogModuleIdentity()` を足した。プロファイル有効化時に1回、**診断スイッチの
外側で**書く:

```
19:51:14.216 [27588] module: C:\Program Files\Ohagey\OhageyTSF.dll  279040 bytes  built 2026-08-14 19:48:49
```

大きさと更新時刻は**そのプロセスに実際にマップされたモジュール**のものなので、
ディスク上のビルドと直接突き合わせられる。

### なぜこれだけスイッチの外なのか

`DiagnosticLog` は入力経路で毎変換2行書くのを止めるためにある。
**これは1プロセス1行**で、打鍵とは無関係である。そして
**この1行が、このファイルの他の全部の行を信用できるものにしている** —
先に有効化しないと読めない情報にしてよいものではない。

`tsf.log` が**無いこと**も情報になる: そのプロセスは計測の入っていないビルドを
持っている、と分かる。今日それで判定した回が実際にあった。

## 追記(2026-08-04、その22)— CI がインストーラを実際にコンパイルする

`iscc` は**誰も CI で走らせていなかった**。守れる範囲は狭いが、
**この決定が守れなかった壊れ方は2回とも黙っていた:**

- `[Files]` の3件中2件が**実在しないパス**を指していた。気づいたのは実機で組んだとき
- `[Setup]` セクションが**丸ごと消えたまま** commit してマージした

**どちらも `iscc` を1回走らせれば即座に落ちる。**

`tsf` ジョブの末尾に足した。あのジョブは既に TSF DLL とエンジンを Release で
組んでいるので、追加で要るのは Inno Setup だけである。出来た成果物が
**1MB を下回ったら失敗**にしてある — ほぼ空の setup.exe は
「`[Files]` が全部 skip された」の見た目であり、緑にしてよい状態ではない。

### 設定アプリだけ CI では外す

WinUI 3 の PRI 生成に Visual Studio の MSBuild が要る。`#ifndef` にしてあるのは
**出荷構成を define の無い側に置く**ためで、外すほうに明示的な指定が要る。
`#ifdef` にすると、指定を忘れた release が設定アプリ抜きで出ていく。

```
iscc /DCiWithoutSettingsApp installer\ohagey.iss   ->  8.7 MB   (CI)
iscc installer\ohagey.iss                          -> 28.0 MB   (出荷)
```

### 自己検査も CI に載せた

`kana-selftest` と `wire-selftest`。どちらもエンジンもパイプもプロファイルも
要らない。ワイヤのほうをここに置いたのは、**更新のたびに必ず混在が起きる**からである
(決定 0032 の追記)。

## 追記(2026-08-05)— バックスペースが**ローマ字1打鍵**を消していた

```
おはぎ  →  バックスペース  →  おはg
```

サンプルは `RemoveVirtualKey(len - 1)` で最後のキーストロークを1つ落とす。
**ピンインではそれが画面上の1文字である** — 1打てば1文字出る。日本語はローマ字で
打ってかなで見るので、**その2つは別の単位**であり、1打鍵だけ消すと音節の途中で
止まる。

判定は `RomajiKana` に純粋な関数として置いた
(`RomajiLengthAfterBackspace`)。TSF もプロファイルもパイプも要らないので
`build-and-run-kana.ps1` で試験できる。**逆引き表は作らない** —
`ohagi` → `ohag` は**表示が3文字のまま**(未解決の子音はそのまま出る)なので、
打鍵を数えても分からない。**結果を数えれば分かる。**

```
ohagi -> おは      ohag -> おは      hon -> ほ
aiueo -> あいうえ  tte  -> t         a   -> (空)
```

### ⚠️ 短い綴りが無い音節は1かなより多く消える

`kyo` は きょ を出すが、**`kyo` のどの接頭辞も き を出さない**ので `k` まで戻る。
き を残すにはバッファを `ki` に**書き換える**ことになり、それは
「打った通りではなくかなを保持する」という別の設計である。
**合成表示とその裏のキーストロークが 食い違いになる**ほうが高くつく。
