<Page
    x:Class="Ohagey.Settings.Pages.DictionaryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ユーザー辞書" Style="{StaticResource TitleTextBlockStyle}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
            <TextBox x:Name="ReadingBox" Header="読み" PlaceholderText="おはぎー" Width="180" />
            <TextBox x:Name="WordBox" Header="単語" PlaceholderText="Ohagey" Width="180" />
            <ComboBox x:Name="PartOfSpeechChoice" Header="品詞" Width="150" />
            <Button x:Name="AddButton" Content="追加" Click="OnAddClicked" VerticalAlignment="Bottom" />
            <Button x:Name="DeleteButton" Content="削除" Click="OnDeleteClicked" VerticalAlignment="Bottom" />
        </StackPanel>

        <InfoBar Grid.Row="2" x:Name="MessageBar" IsOpen="False" IsClosable="True" />

        <ListView Grid.Row="3" x:Name="EntryList" SelectionMode="Single" BorderThickness="1"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid Padding="12,8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="180" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="読み" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock Grid.Column="1" Text="単語" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock Grid.Column="2" Text="品詞" Style="{StaticResource BodyStrongTextBlockStyle}" />
                    </Grid>
                </DataTemplate>
            </ListView.HeaderTemplate>
        </ListView>
    </Grid>
</Page>
